# Server Environment
PORT=5000
NODE_ENV=development

# Database Connection (Neon PostgreSQL)
# For local dev without a PostgreSQL database, leave DATABASE_URL commented out to use the built-in SQLite engine.
# Set your Neon PostgreSQL URL for production deployment:
# DATABASE_URL=postgres://user:password@ep-sample-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# Authentication
JWT_SECRET=super_secret_jwt_key_mini_erp_crm_2026

# CORS Configuration
FRONTEND_URL=http://localhost:5173
